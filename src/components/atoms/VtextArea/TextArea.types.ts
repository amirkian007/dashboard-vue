//*********** */
//Note :  The js docs are generated with Ai
//*********** */

/**
 * Properties for a customizable textarea component.
 */
export interface TextareaProps {
  /**
   * The bound value of the textarea.
   */
  modelValue: string;

  /**
   * Optional label for the textarea, typically displayed above it.
   */
  label?: string;

  /**
   * Placeholder text to display when the textarea is empty.
   */
  placeholder?: string;

  /**
   * Size of the textarea, which can be used to adjust its width and height.
   */
  size?: string;

  /**
   * Whether the textarea is disabled, making it uneditable.
   */
  disabled?: boolean;

  /**
   * Error message to display when there’s a validation error.
   */
  error?: string;
  /**
   * is input required.
   */
  required?: boolean;
    
}
