//*********** */
//Note :  The js docs are generated with Ai
//*********** */

/**
 * Props for a Modal component.
 */
export interface ModalProps {
  /**
   * The title of the modal, displayed at the top.
   */
  title: string;

  /**
   * Controls the visibility of the modal.
   * When `true`, the modal is visible; when `false`, it is hidden.
   * Typically used with `v-model` to sync state.
   */
  modelValue: boolean;
}
