//*********** */
//Note :  The js docs are generated with Ai
//*********** */

/**
 * Properties for a customizable checkbox component.
 */
export interface CheckBoxProps {
  /**
   * Label displayed next to the checkbox.
   */
  label: string;

  /**
   * The bound value of the checkbox; `true` if checked, `false` if unchecked.
   */
  modelValue: boolean;
}
